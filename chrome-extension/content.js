// content.js – roda no contexto da página do Atendare
// Este arquivo fica em standby; a extração real é feita via executeScript em popup.js.
// Mantenha aqui caso queira adicionar observadores de DOM ou lógica contínua.

(function () {
  // Sinaliza que o content script está ativo
  window.__acomSyncReady = true;
})();
